services:
  fastapi:
    build: ./fastapi
    container_name: fastapi
    ports:
      - "8000:8000"
    environment:
      AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
      AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
      AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION}"
      S3_BUCKET_NAME: "${S3_BUCKET_NAME}"

  # postgres:
  #   image: postgres:15
  #   container_name: postgres
  #   restart: always
  #   ports:
  #     - "5432:5432"
  #   environment:
  #     POSTGRES_USER: admin
  #     POSTGRES_PASSWORD: admin
  #     POSTGRES_DB: diabetesdb
  #   volumes:
  #     - ./postgres/data:/var/lib/postgresql/data

  # mlflow:
  #   build:
  #     context: ./mlflow
  #     dockerfile: Dockerfile
  #   container_name: mlflow
  #   ports:
  #     - "5500:5000"
  #   environment:
  #     MLFLOW_BACKEND_STORE_URI: "postgresql://admin:admin@postgres:5432/diabetesdb"
  #     MLFLOW_ARTIFACT_URI: "s3://mlflow-artifacts"   # bucket do seu amigo
  #     AWS_ACCESS_KEY_ID: "${AWS_ACCESS_KEY_ID}"
  #     AWS_SECRET_ACCESS_KEY: "${AWS_SECRET_ACCESS_KEY}"
  #     AWS_DEFAULT_REGION: "${AWS_DEFAULT_REGION}"
  #   depends_on:
  #     - postgres
  #   command: >
  #     mlflow server
  #     --backend-store-uri postgresql://admin:admin@postgres:5432/diabetesdb
  #     --artifacts-destination s3://mlflow-artifacts
  #     --host 0.0.0.0
  #     --port 5000

  jupyterlab:
    build:
      context: ./jupyter-lab
      dockerfile: Dockerfile
    container_name: jupyterlab
    ports:
      - "8888:8888"
    volumes:
      - ./jupyter-lab/notebooks:/home/jovyan/work
    environment:
      JUPYTER_ENABLE_LAB: "yes"
    # depends_on:
    #   - postgres
    #   - mlflow

networks:
  default:
    name: diabetes_net
